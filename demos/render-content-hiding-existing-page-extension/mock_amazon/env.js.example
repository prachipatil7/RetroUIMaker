/**
 * Environment Configuration Template
 * 
 * Copy this file to env.js and set your API keys.
 * The env.js file should not be committed to version control.
 */

window.ENV = {
  // OpenAI API Key for LLM operations
  OPENAI_API_KEY: "sk-your-openai-api-key-here"
};

// Alternative: Set via Chrome storage (can be done programmatically)
// chrome.storage.local.set({ openaiApiKey: "sk-your-openai-api-key-here" });
